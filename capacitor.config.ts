import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ricardoramos.comerciolocal.com',
  appName: 'Comercio Local',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
