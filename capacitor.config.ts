import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Comercio Local',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
